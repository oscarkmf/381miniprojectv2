<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <title><%= rest.name %></title>
    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 100%;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
      }
    </style>
  </head>
  <body>
	<div id="demo">
		<H1>Map for <%= rest.address.coord %> </H1>
	</div>
    <div id="map"></div>
    <script>

      function initMap() {
        var myLatLng = {lat: <%= rest.address.coord[0] %>, lng: <%= rest.address.coord[1] %>};
        });
      }
    </script>
    <script async defer
    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAFaq-mMt1T9E69J1uRBmajEQC2T8yfAOc&callback=initMap">
    </script>
  </body>
</html>